; ENGLISH.FS  28-NOV-95
; based on Englex 2.0b5 (or later) using gloss field gl2

; Input to the file synthesize command.
; To run this file, load ENGLISH2.LEX as the synthesis-lexicon and then
;  enter the command FILE SYNTHESIZE ENGLISH

`cat
`cat +PL
`cat +GEN
`cat +PL +GEN
`fox
`fox +PL
`fox +GEN
`fox +PL +GEN
`kiss
`kiss +PL
`kiss +3SG
`kiss +GEN
`kiss +PL +GEN
`kiss +ED
`kiss +EN
`kiss +ING
`kiss +AJR25a
`church
`church +PL
`church +GEN
`church +PL +GEN
`lash
`lash +PL
`lash +3SG
`lash +GEN
`lash +PL +GEN
`ski
`ski +PL
`ski +3SG
`ski +GEN
`ski +PL +GEN
`ski +ED
`ski +EN
`ski +ING
`ski +AJR25a
`ski +NR19
`ski +NR19 +PL
`ski +NR19 +GEN
`ski +NR19 +PL +GEN
`mouse
`mouse +GEN
`mice
`mice +GEN
`ice
`ice +GEN
`boy
`boy +PL
`boy +GEN
`boy +PL +GEN
`spy
`spy +PL
`spy +3SG
`spy +GEN
`spy +PL +GEN
`spy +ED
`spy +EN
`spy +ING
`fly +PL
`fly +3SG
`being
`try
`try +PL
`try +3SG
`try +ED
`try +EN
`try +ING
re`ly +AJR25a
`tie
`tie +PL
`tie +3SG
`tie +ED
`tie +EN
`tie +ING
`tie +AJR25a
REV1+ `tie
REV1+ `tie +3SG
NEG4+ `tie +EN
REV1+ `tie +ED
REV1+ `tie +EN
NEG4+ `tie +ING
REV1+ `tie +ING
NEG4+ `tie +AJR25a
REV1+ `tie +AJR25a
`go
`go +3SG
`go +ING
`went
`gone
`sleep
`sleep +PL
`sleep +3SG
`sleep +ING
`slept
`slip
`slip +PL
`slip +3SG
`slip +ING
`slip +ED
`slip +EN
`hop +ED
`hop +EN
`hope +ED
`hope +EN
`rub +ING
`rubbish
re`fer
re`fer +3SG
re`fer +ING
re`fer +ED
re`fer +EN
re`fer +AJR25a
`travel
`travel +PL
`travel +3SG
`travel +ING
`travel +ED
`travel +EN
`travel +NR19
`move
`move +PL
`move +3SG
`move +ING
`move +ED
`move +EN
`move +AJR25a
be`lieve
be`lieve +3SG
be`lieve +ED
be`lieve +EN
be`lieve +ING
be`lieve +NR19
be`lieve +AJR25a
`trace
`trace +PL
`trace +3SG
`trace +ING
`trace +ED
`trace +EN
`trace +AJR25a
ORD5+ `trace
ORD5+ `trace +3SG
ORD5+ `trace +ING
ORD5+ `trace +ED
ORD5+ `trace +EN
ORD5+ `trace +AJR25a
`manage
`manage +3SG
`manage +ING
`manage +ED
`manage +EN
`manage +AJR25a
`manage +NR19
`change
`change +PL
`change +3SG
`change +ING
`change +ED
`change +EN
`change +AJR25a
`change +NR19
a`gree
a`gree +3SG
a`gree +ED
a`gree +EN
a`gree +ING
a`gree +AJR25a
REV3+ a`gree
REV3+ a`gree +3SG
REV3+ a`gree +ED
REV3+ a`gree +EN
REV3+ a`gree +ING
REV3+ a`gree +AJR25a
refer`ee
refer`ee +PL
refer`ee +3SG
refer`ee +GEN
refer`ee +PL +GEN
refer`ee +ED
refer`ee +EN
refer`ee +ING
refer`ee +AJR25a
`hoe
`hoe +PL
`hoe +3SG
`hoe +ED
`hoe +EN
`hoe +ING
`hoe +AJR25a
`dye
`dye +PL
`dye +3SG
`dye +ED
`dye +EN
`dye +ING
`dye +AJR25a
`argue
`argue +3SG
`argue +ED
`argue +EN
`argue +ING
`argue +AJR25a
`clear
NEG4+ `clear
`clear +AVR1
`happy
NEG4+ `happy
`happy +AVR1
NEG4+ `happy +AVR1
`happy +CMP
NEG4+ `happy +CMP
NEG4+ `happy +SPR
`real
NEG4+ `real
ORD5+ al`ly
`real +AVR1
`clear +NR19
`clear +CMP
`clear +SPR
`big
`big +CMP
`big +SPR
`cool
`cool +NR19
`cool +CMP
`cool +SPR
`red
`red +CMP
`red +SPR
`red +NR27
`boy +AJR10
`boy +AJR10 +NR27
`spot +AJR14
`spot +AJR14 +NR27
`friend +AJR13
`friend +AJR13 +NR27
REV1+ `tie
REV1+ `tie +3SG
NEG4+ `tie +EN
REV1+ `tie +ED
REV1+ `tie +EN
NEG4+ `tie +ING
REV1+ `tie +ING
NEG4+ `tie +AJR25a
REV1+ `tie +AJR25a
NEG4+ ex`pect +EN
NEG4+ ex`pect +EN +AVR1
`write +ING
`written
NEG4+ `written
ORD5+ `written
`write +ING +PL
ORD5+ `categorize
ORD5+ `tie +ED
ORD5+ `tie +EN
ORD5+ `tie +ING
sur`prise +ING +AVR1
NEG4+ sur`prise +ING +AVR1
PEJ2+ `give +ING +PL
ORD5+ `analyze
ORD5+ a`nalysis
ORD5+ `calculable
NEG2a+ `calculable
NEG2a+ `accuracy
VR1a+ `large
VR1a+ `large +NR25
